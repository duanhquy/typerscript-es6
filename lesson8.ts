var calculateSalary = function(){
    return 100000;
}

let calculateSalaryArrow = (bonus:number,tax:number) => 20000 + bonus;

console.log(calculateSalary());
console.log(calculateSalaryArrow(1000,1500));